const HireMe: React.FC = () => {
    return (
        <button to="connect" smooth={true} duration={800} className="w-30 min-w-max py-2 text-_black font-semibold hover:opacity-90 bg-_orange px-8 rounded-full">
            Hire Me
        </button>
    );
};

export default HireMe;
